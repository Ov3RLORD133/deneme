# KeyChaser Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          KEYCHASER ARCHITECTURE                              │
│                   Malware C2 Sinkhole & Analysis Framework                  │
└─────────────────────────────────────────────────────────────────────────────┘

                              INCOMING MALWARE TRAFFIC
                                        │
                    ┌───────────────────┴───────────────────┐
                    │                                       │
             ┌──────▼────────┐                    ┌────────▼──────┐
             │  Port 4444    │                    │  Port 5555    │
             │ ExampleLogger │                    │ YourMalware   │
             │   Protocol    │                    │   Protocol    │
             └──────┬────────┘                    └────────┬──────┘
                    │                                       │
                    └───────────────┬───────────────────────┘
                                    │
                    ┌───────────────▼────────────────┐
                    │   PROTOCOL HANDLER LAYER       │
                    │  (ProtocolHandler Base Class)  │
                    │                                │
                    │  ┌──────────────────────────┐  │
                    │  │  1. Receive Connection   │  │
                    │  │  2. Read Raw Bytes       │  │
                    │  │  3. Decrypt Payload      │  │
                    │  │  4. Parse Structure      │  │
                    │  │  5. Extract Intel        │  │
                    │  └──────────────────────────┘  │
                    └───────────────┬────────────────┘
                                    │
                    ┌───────────────▼────────────────┐
                    │     CRYPTOGRAPHY LAYER         │
                    │                                │
                    │  ┌─────────────────────────┐   │
                    │  │ • XOR Decryption        │   │
                    │  │ • RC4 Stream Cipher     │   │
                    │  │ • AES-128/256 (CBC/ECB) │   │
                    │  │ • PKCS7 Padding         │   │
                    │  │ • Hexdump Formatter     │   │
                    │  └─────────────────────────┘   │
                    └───────────────┬────────────────┘
                                    │
                    ┌───────────────▼────────────────┐
                    │     DATA EXTRACTION LAYER      │
                    │                                │
                    │  ┌─────────────────────────┐   │
                    │  │ Bot Info:               │   │
                    │  │  • Bot ID, IP, Port     │   │
                    │  │  • Hostname, Username   │   │
                    │  │  • OS Info, Version     │   │
                    │  │                         │   │
                    │  │ Logs:                   │   │
                    │  │  • Keystrokes           │   │
                    │  │  • Window Titles        │   │
                    │  │  • Screenshots          │   │
                    │  │                         │   │
                    │  │ Credentials:            │   │
                    │  │  • Passwords            │   │
                    │  │  • Tokens, Cookies      │   │
                    │  │  • Browser Saved Creds  │   │
                    │  └─────────────────────────┘   │
                    └───────────────┬────────────────┘
                                    │
                    ┌───────────────▼────────────────┐
                    │    DATABASE LAYER (SQLite)     │
                    │     (Async SQLAlchemy ORM)     │
                    │                                │
                    │  ┌──────────┐  ┌──────────┐   │
                    │  │   Bots   │  │   Logs   │   │
                    │  │  Table   │  │  Table   │   │
                    │  └──────────┘  └──────────┘   │
                    │  ┌──────────────────────────┐  │
                    │  │   Credentials Table      │  │
                    │  └──────────────────────────┘  │
                    └───────────────┬────────────────┘
                                    │
                    ┌───────────────▼────────────────┐
                    │       REST API LAYER           │
                    │        (FastAPI)               │
                    │                                │
                    │  ┌──────────────────────────┐  │
                    │  │ GET  /api/bots           │  │
                    │  │ GET  /api/logs           │  │
                    │  │ GET  /api/stats/overview │  │
                    │  │ GET  /health             │  │
                    │  └──────────────────────────┘  │
                    └───────────────┬────────────────┘
                                    │
                    ┌───────────────▼────────────────┐
                    │      DASHBOARD (Port 8000)     │
                    │   (Jinja2 + TailwindCSS)       │
                    │                                │
                    │  ┌──────────────────────────┐  │
                    │  │  📊 Statistics Cards     │  │
                    │  │  • Total Bots            │  │
                    │  │  • Active Infections     │  │
                    │  │  • Captured Keystrokes   │  │
                    │  │  • Stolen Credentials    │  │
                    │  └──────────────────────────┘  │
                    │                                │
                    │  ┌──────────────────────────┐  │
                    │  │  📋 Recent Bots Table    │  │
                    │  │  Bot ID | IP | Protocol  │  │
                    │  └──────────────────────────┘  │
                    │                                │
                    │  ┌──────────────────────────┐  │
                    │  │  ⌨️  Keystroke Logs       │  │
                    │  │  Window | Keys | Time    │  │
                    │  └──────────────────────────┘  │
                    │                                │
                    │  🔄 Auto-refresh: 5 seconds    │
                    └────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                          CONCURRENT OPERATIONS                               │
└─────────────────────────────────────────────────────────────────────────────┘

                         ┌──────────────────┐
                         │  asyncio.gather  │
                         │  (Event Loop)    │
                         └────────┬─────────┘
                                  │
                ┌─────────────────┼─────────────────┐
                │                 │                 │
        ┌───────▼────────┐ ┌─────▼──────┐ ┌───────▼────────┐
        │  FastAPI       │ │ Protocol   │ │ Protocol       │
        │  HTTP Server   │ │ Listener 1 │ │ Listener 2     │
        │  (Port 8000)   │ │ (Port 4444)│ │ (Port 5555)    │
        └────────────────┘ └────────────┘ └────────────────┘
                │                 │                 │
                └─────────────────┼─────────────────┘
                                  │
                        Async Database I/O
                        (Non-blocking)


┌─────────────────────────────────────────────────────────────────────────────┐
│                           FILE STRUCTURE MAP                                 │
└─────────────────────────────────────────────────────────────────────────────┘

KeyChaser/
│
├── app/main.py ─────────────────────► Entry Point (Orchestrator)
│                                      • Loads protocol handlers
│                                      • Spawns async listeners
│                                      • Starts FastAPI server
│
├── app/protocols/
│   ├── base.py ─────────────────────► Abstract Base Class
│   │                                  • decrypt() method
│   │                                  • parse() method
│   │                                  • handle_connection()
│   │
│   ├── utils.py ────────────────────► Crypto Utilities
│   │                                  • xor_decrypt()
│   │                                  • rc4_decrypt()
│   │                                  • aes_decrypt()
│   │                                  • hexdump()
│   │
│   └── example_logger.py ───────────► Example Implementation
│       └── YourHandler.py ──────────► Your Custom Handler
│
├── app/models/
│   ├── bot.py ──────────────────────► Bot ORM Model + Schemas
│   ├── log.py ──────────────────────► Log ORM Model + Schemas
│   └── credential.py ───────────────► Credential ORM Model + Schemas
│
├── app/api/
│   ├── bots.py ─────────────────────► Bot CRUD Endpoints
│   ├── logs.py ─────────────────────► Log Query Endpoints
│   └── stats.py ────────────────────► Statistics Endpoints
│
├── app/core/
│   ├── config.py ───────────────────► Pydantic Settings
│   ├── database.py ─────────────────► SQLAlchemy Setup
│   └── logging.py ──────────────────► Logging Configuration
│
└── app/templates/
    └── dashboard.html ──────────────► Frontend Dashboard


┌─────────────────────────────────────────────────────────────────────────────┐
│                          DATA FLOW EXAMPLE                                   │
└─────────────────────────────────────────────────────────────────────────────┘

1. Malware Beacon Arrives
   ───────────────────────
   Raw Encrypted Bytes: [0x54, 0x45, 0x53, 0x54, ...]
                        │
                        ▼
2. Protocol Handler Receives
   ──────────────────────────
   handler.handle_connection(reader, writer)
   • Extracts client IP: 192.168.1.100
   • Reads payload: 141 bytes
                        │
                        ▼
3. Decryption
   ───────────
   xor_decrypt(data, b"SecretKey123")
   Result: "BOT001|DESKTOP-PC|john.doe|Windows 10|..."
                        │
                        ▼
4. Parsing
   ────────
   extract_delimited_strings(data, "|")
   • bot_id = "BOT001"
   • hostname = "DESKTOP-PC"
   • keystrokes = "password123"
                        │
                        ▼
5. Database Storage
   ────────────────
   INSERT INTO bots (bot_id, ip_address, hostname, ...)
   INSERT INTO logs (bot_id, keystroke_data, ...)
                        │
                        ▼
6. Dashboard Display
   ─────────────────
   JavaScript fetch('/api/bots')
   • Shows in "Recent Infections" table
   • Updates statistics cards
   • Auto-refreshes every 5 seconds


┌─────────────────────────────────────────────────────────────────────────────┐
│                        SECURITY FEATURES                                     │
└─────────────────────────────────────────────────────────────────────────────┘

🛡️  Rate Limiting        │ Max 10 connections per IP
🛡️  Packet Size Limits   │ 64KB maximum payload
🛡️  Connection Timeouts   │ 30 second timeout
🛡️  Input Validation      │ Pydantic schema validation
🛡️  Isolated Database     │ SQLite file-based (no network exposure)
🛡️  Structured Logging    │ Separate traffic.log for forensics
🛡️  Error Handling        │ Try/except with graceful recovery


┌─────────────────────────────────────────────────────────────────────────────┐
│                         DEPLOYMENT OPTIONS                                   │
└─────────────────────────────────────────────────────────────────────────────┘

Option 1: Direct Python
────────────────────────
$ python -m app.main
✅ Simple, fast startup
✅ Full access to logs
✅ Easy debugging

Option 2: Docker Compose
─────────────────────────
$ docker-compose up -d
✅ Isolated environment
✅ Portable deployment
✅ Easy scaling

Option 3: Production (Gunicorn)
────────────────────────────────
$ gunicorn -w 4 -k uvicorn.workers.UvicornWorker app.main:app
✅ Multiple workers
✅ Better performance
✅ Production-ready
